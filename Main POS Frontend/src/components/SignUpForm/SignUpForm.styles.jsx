import styled from "styled-components";
import {LoginContainer} from "./../LoginForm/LoginForm.styles"

export const SignUpContainer = styled(LoginContainer)`
    a {
        font-size : 1.5rem;
    }
`
